{
  "name": "@flowsave/core",
  "version": "0.1.0",
  "private": true,
  "description": "Flowsave core — shared business logic for CLI, agent, and dashboard",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run --reporter=verbose",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^4.1.0"
  }
}
